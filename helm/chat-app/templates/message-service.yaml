apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-service
spec:
  replicas: {{ .Values.messageService.replicas }}
  selector:
    matchLabels:
      app: message-service
  template:
    metadata:
      labels:
        app: message-service
    spec:
      containers:
        - name: message-service
          image: {{ .Values.messageService.image }}
          imagePullPolicy: Never
          ports:
            - containerPort: {{ .Values.messageService.port }}
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://{{ .Values.database.host }}:{{ .Values.database.port }}/{{ .Values.database.dbName }}"
            - name: SPRING_DATASOURCE_USERNAME
              value: {{ .Values.database.username }}
            - name: SPRING_DATASOURCE_PASSWORD
              value: {{ .Values.database.password }}
            - name: USER_SERVICE_URL
              value: "http://user-service:{{ .Values.userService.port }}/api/v1/users"
            - name: CHATROOM_SERVICE_URL
              value: "http://chatroom-service:{{ .Values.chatroomService.port }}/api/v1/chatrooms"
          readinessProbe:
            tcpSocket:
              port: {{ .Values.messageService.port }}
            initialDelaySeconds: 45
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: {{ .Values.messageService.port }}
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: message-service
spec:
  selector:
    app: message-service
  ports:
    - protocol: TCP
      port: {{ .Values.messageService.port }}
      targetPort: {{ .Values.messageService.port }}